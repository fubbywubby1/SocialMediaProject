import java.net.URL;
public class Ad extends MediaItem {
    private URL URL;

    public Ad(String title, String description, URL URL) {
        super(title, description);
        this.URL = URL;
    }

    public URL getURL() {
        return URL;
    }

    public void setURL(URL newURL) {
        this.URL = newURL;
    }
}
